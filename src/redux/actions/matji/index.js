import {types} from '../../types/mtaji';

export const loadAllPartnersAction = () => ({
	type: types.LOAD_ALL_PARTNERS,
	payload: {},
});

export const loadAllCustomersAction = () => ({
	type: types.LOAD_ALL_CUSTOMERS,
	payload: {},
});
